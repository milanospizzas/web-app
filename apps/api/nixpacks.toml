[providers]
node = "20"

[phases.setup]
aptPkgs = []
nixPkgs = ["pnpm"]


[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"
